<!-- ghyll bias — edit/delete as needed. -->
# /status — orient on session start.

Run `/list-arrows` + `ghyll arrow show <next-likely-id>`. Report in one paragraph: which arrows are open, which clauses are `unevaluated`, which findings unresolved. Don't act yet — just say what you see.
